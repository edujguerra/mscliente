server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pedidos
    username: root
    password: password
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      format_sql: true
    generate-ddl: true
    show-sql: true